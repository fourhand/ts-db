[package]
name = "ts-db"
version = "0.1.0"
edition = "2024"

[dependencies]
memmap2 = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde", "clock" ] }

[dev-dependencies]
tempfile = "3"
